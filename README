
sudo docker build -t hello567 .
#docker run -p 80:80 hello567
docker run -p 80:80 -v ~/mount_test/src:/usr/share/nginx/html hello567

# open web explore
#enter localhost:80

# change code in index.html
#refresh the webpage
